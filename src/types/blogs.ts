export type BlogsViewModel = {
    id:	string
    name: string
    description: string
    websiteUrl:	string
}

export type BlogsInputModel = {
    name: string
    description: string
    websiteUrl: string
}