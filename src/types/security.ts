export type DeviceViewModel = {
    ip: string,
    title: string,
    lastActiveDate: string,
    deviceId: string
}

export type DeviceModel = {
    ip: string,
    title: string,
    lastActiveDate: string,
    expiredTokenDate: string,
    deviceId: string,
    userId: string,
}